
import { ProjectWrapper } from '../components/ProjectWrapper'
import CTA from '../components/CTA'
import { Github, Earth } from 'lucide-react'
import { CldImage } from 'next-cloudinary'
import { ProjectBanner } from '../components/project/Banner'

<ProjectWrapper>
  {/*
    Define the project object so it can be used below.
    Add a label if you want it to show up.
  */}

      **bold text**
  {(() => {
    const project = {
      image: "/projects/Digital_Patient_Twin_hr0whf",
      title: "Digital Patient Twin",
      label:"Industry Project",
      alt: "Digital Patient Twin Banner",
      tags: ["Azure", "CosmosDB", "Dapr", "Microservices", "MQTT", "Nuxt", "TypeScript"],
    
    };
    return (
      <>

        <ProjectBanner project={project} />
        {project.label && (
          <span className="inline-block mb-4 px-3 py-1 text-sm font-semibold rounded-full bg-teal-100 text-teal-700 dark:bg-teal-900 dark:text-teal-300">
            {project.label}
          </span>
        )}

        <h2 className="text-2xl font-bold mb-4">
          <span className="bg-clip-text text-transparent bg-gradient-to-r from-teal-400 via-cyan-500 to-blue-600 dark:from-teal-300 dark:via-cyan-400 dark:to-blue-400">
            Digital Patient Twin
          </span>
        </h2>

        <div className="flex flex-wrap gap-2 mb-6">
          {project.tags.map(tag => (
            <span
              key={tag}
              className="px-3 py-1 text-xs sm:text-sm font-mono bg-gray-200 dark:bg-slate-800 rounded-full"
            >
              {tag}
            </span>
          ))}
        </div>

        {/* Description */}
          <p className="text-md leading-relaxed dark:text-slate-400 font-medium">
            During Industry Project I was assigned with a team of talented developers: <strong className="text-teal-500 dark:text-teal-400">Jarne Delarue</strong> and <strong className="text-teal-500 dark:text-teal-400">Aléssia Vinnik</strong> and worked with  **Delaware**. The Digital Patient Twin is a real-time monitoring dashboard designed to empower doctors with instant access to patient data. By providing a user-friendly interface, doctors can efficiently manage patient information and enhance patient care. Collaborating closely with my team members, I integrated backend data seamlessly into the frontend. This cohesive approach ensures that doctors receive accurate and up-to-date patient information. Due to company policy I can unfortunately not display the backend code online.
          </p>

          
       
        <div className="mt-6 flex gap-4">
          <CTA href="https://github.com/JarneDel/digital-patient-twin-frontend" target="_blank">
            <Github size={32} />
          </CTA>
         
        </div>
      </>
    );
  })()}
</ProjectWrapper>







